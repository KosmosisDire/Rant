/* The native plugin translation unit: emits the implementation from the amalgamation
 * into dart.dll, libdart.so or libdart.dylib. build.ps1 and build.sh compile it. */
#define DART_IMPLEMENTATION
#include "dart.h"
