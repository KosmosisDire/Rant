/* The DART implementation TU: the single-header library is C99, so it compiles here
 * as C and bridge.cpp links against it through dart.h's extern "C" declarations. */
#define DART_IMPLEMENTATION
#include "dart.h"
