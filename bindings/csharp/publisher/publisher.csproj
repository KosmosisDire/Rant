<Project Sdk="Microsoft.NET.Sdk">

  <!-- 1000 Hz publisher on the default interface/domain. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
    <AssemblyName>rant_publisher</AssemblyName>
    <RootNamespace>RantPublisher</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="../Rant.cs" />
    <None Include="../../../dist/native/**/*" Link="%(Filename)%(Extension)">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
