<Project Sdk="Microsoft.NET.Sdk">

  <!-- 1000 Hz publisher on the default interface/domain. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
    <AssemblyName>ramble_publisher</AssemblyName>
    <RootNamespace>RamblePublisher</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="../Ramble.cs" />
    <None Include="../../../dist/native/**/*" Link="%(Filename)%(Extension)">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
