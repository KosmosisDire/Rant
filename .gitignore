old
*.exe
/build/

/Debug/
# cross-compiled / native binaries (no extension), repo-root only so they do not
# also match source dirs like src/node/
/rant_test
/rant_test_noshm
/example
/node
/pack
/pubsub
*.png
*.jpg
*.pdb
# C# / .NET wrapper build output (bindings/csharp/*/{bin,obj}); the wrapper source is bindings/csharp/Rant.cs
[Bb]in/
[Oo]bj/
# The release workflow's staging folders
/artifacts/
/wheelhouse/
/release/
# dist/ is all build output: the single headers packed from src/, the shared library per
# platform and the packages. Releases attach them. Never committed.
/dist/
# Python build/cache artifacts (pip build, bytecode)
__pycache__/
*.egg-info/