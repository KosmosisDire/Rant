old
*.exe
/build/

/Debug/
# cross-compiled / native binaries (no extension), repo-root only so they do not
# also match source dirs like src/node/
/ramble_test
/ramble_test_noshm
/example
/node
/pack
/pubsub
*.png
*.jpg
# explorer UI: handoff package + fetched reference docs + standalone build tree
/Ramble Network Debugging Interface.zip
/_unzipped/
/_ref/
/explore/build/
*.pdb
# C# / .NET wrapper build output (csharp/*/{bin,obj}); the wrapper source is csharp/Ramble.cs
[Bb]in/
[Oo]bj/
# The release workflow's staging folders
/artifacts/
/wheelhouse/
/release/
# The shared library the CMake target ramble_shared builds, one folder per platform. The
# packages bundle it. Never committed.
/dist/native/
# Packages built into dist/ by dotnet pack, pip wheel and tools/unity.cmake. Never committed.
/dist/*.nupkg
/dist/*.whl
/dist/*.tar.gz
/dist/*.unitypackage
/dist/com.rant.ramble/
# Python build/cache artifacts (pip build, bytecode)
__pycache__/
*.egg-info/