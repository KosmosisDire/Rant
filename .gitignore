old
*.exe
/build/

/Debug/
# cross-compiled / native binaries (no extension), repo-root only so they do not
# also match source dirs like src/node/
/dart_test
/dart_test_noshm
/example
/node
/pack
/pubsub
*.png
*.jpg
# explorer UI: handoff package + fetched reference docs + standalone build tree
/DART Network Debugging Interface.zip
/_unzipped/
/_ref/
/explore/build/
*.pdb
# C# / .NET wrapper build output (csharp/*/{bin,obj}); the wrapper source is csharp/Dart.cs
[Bb]in/
[Oo]bj/
# NuGet pack output + CI release staging
/nupkg/
/release/
# The shared library the CMake target dart_shared builds, one folder per platform. The
# packages bundle it. Never committed.
/dist/native/
# Packages built into dist/ by dotnet pack and pip wheel. Never committed.
/dist/*.nupkg
/dist/*.whl
/dist/*.tar.gz
/csharp/unity/Runtime/Plugins/
# The Unity package's Dart.cs is a copy of csharp/Dart.cs made by unity/pack.{ps1,sh}
# (one source of truth, no committed duplicate).
/csharp/unity/Runtime/Dart.cs
# Python build/cache artifacts (pip build, bytecode)
__pycache__/
*.egg-info/