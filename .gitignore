old
*.exe
/build/

/Debug/
# cross-compiled / native binaries (no extension), repo-root only so they do not
# also match source dirs like src/node/
/ramble_test
/ramble_test_noshm
/example
/node
/pack
/pubsub
*.png
*.jpg
# explorer UI: handoff package + fetched reference docs + standalone build tree
/Ramble Network Debugging Interface.zip
/_unzipped/
/_ref/
/explore/build/
*.pdb
# C# / .NET wrapper build output (bindings/csharp/*/{bin,obj}); the wrapper source is bindings/csharp/Ramble.cs
[Bb]in/
[Oo]bj/
# The release workflow's staging folders
/artifacts/
/wheelhouse/
/release/
# dist/ is all build output: the single headers packed from src/, the shared library per
# platform and the packages. Releases attach them. Never committed.
/dist/
# Python build/cache artifacts (pip build, bytecode)
__pycache__/
*.egg-info/