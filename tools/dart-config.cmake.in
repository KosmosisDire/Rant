@PACKAGE_INIT@

# dart_host carries Threads::Threads, so the finder has to run before the targets file.
include(CMakeFindDependencyMacro)
find_dependency(Threads)

include("${CMAKE_CURRENT_LIST_DIR}/dart-targets.cmake")
